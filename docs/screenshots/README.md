# Control-plane TUI evidence

`control-plane-tui.png` is a browser-rendered ANSI terminal capture of the actual
TUI connected with `--server` to an isolated control-plane server. It shows
`/help`, including stop, settle, explicit unsettle, and continue. The terminal
capture preserves the TUI's ANSI formatting; no model requests or workspace
changes were made to create this evidence.

# Web grid evidence

These captures use deterministic mocked API/WebSocket data, not live projects or
paid model turns. Generated by `TED_WEB_RECORD=1 npx playwright test grid.spec.ts`
in `web/` after the Inter / IBM Plex Mono grid rollout.

- [Desktop chat, light, 1440px](web-grid-desktop-chat.png)
- [Desktop project defaults, light, 1440px](web-grid-desktop-settings.png)
- [Mobile chat, dark, 390px](web-grid-mobile-chat.png)
- [Mobile project defaults, dark, 390px](web-grid-mobile-settings.png)
- [Mobile chat → project defaults → effort selection recording](web-grid-mobile-demo.webm)

Context is read-only footer metadata. There is no agent settings dialog. The
recording opens project defaults through the sidebar; per-chat model/effort
controls remain in the composer.
The test suite also attaches captures at 320, 768, and 1920px in both themes.
Older screenshots in this directory document earlier work, not the current grid.

# Tool/sidebar iPhone pass

These captures use **WebKit with iPhone 13 emulation**, in light and dark themes,
from `TED_WEB_RECORD=1 npm run test:iphone`. The data is mocked as above.

- [Selected sidebar chat — light](web-iphone-sidebar-light.png)
- [Selected sidebar chat — dark](web-iphone-sidebar-dark.png)
- [Tool waiting for output](web-iphone-tool-waiting.png)
- [Tool output available and expanded](web-iphone-tool-result.png)
- [Long draft in a short 320×480 viewport](web-iphone-short-viewport.png)
- [Sidebar, spinner/result transition, short viewport, and project defaults recording](web-iphone-demo.webm)

The short viewport is a controlled resize, **not** a native iOS keyboard capture.
Actual software-keyboard occlusion and Safari toolbar behavior still require a
real iPhone check. The selected title and branch meet 4.5:1 text contrast in both
themes in the focused browser tests.

Project settings now share the chat settle/restore reveal behavior:
[desktop hover capture](web-project-action-hover.png) and
[hover/keyboard/collapsed-project recording](web-project-action-demo.webm).
The iPhone sidebar captures above verify that project settings remain visible
with a 48px touch target, without requiring hover.

# Sending from an already-bottom viewport

The regression reproduces a scroller state that is paused by a horizontal/touch
intent even though the transcript is still at its bottom. Previously, sending
then left the viewport 198px above the new end in this fixture. Successful local
sends now explicitly resume following through subsequent content/layout updates.

- [Before: new content remains below the viewport](web-send-scroll-before.png)
- [After: sending resumes bottom-following](web-send-scroll-after.png)
- [Fixed send flow recording](web-send-scroll-demo.webm)

These captures use Chromium with an iPhone-sized viewport and a replayed
`touchmove` handler, not a physical iPhone gesture. The same regressions run in
WebKit through `npm run test:iphone`; mobile reading-position setup replays touch
intent and scroll position because Playwright has no touch-pan API. Separate
cases cover composer shrink, queued turns, failed/slow sends, reading during a
pending request, and late acknowledgements after changing chats.

# Chat worktrees

These captures use the real control-plane HTTP/WebSocket server and Git, with a
local bare remote, an isolated temporary checkout/state directory, and a
deterministic fake provider. The provider executes a real Bash tool in the
created worktree. No paid model calls or modifications to real projects were
used. The original checkout contains a local edit and `.env`; neither is copied
to or modified by the worktree chat.

- [Project workspace defaults](web-worktree-settings.png)
- [Draft mode and per-chat starting-branch override](web-worktree-draft.png)
- [Locked workspace and real tool output](web-worktree-ready.png)
- [Mobile workspace indicator](web-worktree-mobile.png)
- [Permanent setup failure, with no retry](web-worktree-failed.png)
- [Project defaults → per-chat override → first send → locked workspace recording](web-worktree-demo.webm)

The mobile image is a Chromium 390×844 viewport, not a physical-phone capture.
